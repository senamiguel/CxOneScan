<Window x:Class="CxDesktopWrapper.CopilotFilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Seleção de Vulnerabilidades para o Copilot" Height="600" Width="1000" WindowStartupLocation="CenterOwner" Background="{DynamicResource ApplicationPageBackgroundThemeBrush}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Filtros Rápidos" FontWeight="Bold" FontSize="16" Foreground="White" Margin="0,0,0,10"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Severidade:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            <CheckBox x:Name="chkCritical" Content="Critical" IsChecked="True" Margin="0,0,10,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
            <CheckBox x:Name="chkHigh" Content="High" IsChecked="True" Margin="0,0,10,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
            <CheckBox x:Name="chkMedium" Content="Medium" IsChecked="True" Margin="0,0,10,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
            <CheckBox x:Name="chkLow" Content="Low / Info" IsChecked="True" Margin="0,0,20,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>

            <TextBlock Text="Tipo:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            <CheckBox x:Name="chkSast" Content="SAST" IsChecked="True" Margin="0,0,10,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
            <CheckBox x:Name="chkSca" Content="SCA" IsChecked="True" Margin="0,0,10,0" Checked="Filter_Changed" Unchecked="Filter_Changed"/>
        </StackPanel>

        <DataGrid x:Name="dgVulnerabilities" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" RowBackground="Transparent" AlternatingRowBackground="{DynamicResource ControlFillColorDefaultBrush}" BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="40" ElementStyle="{DynamicResource DefaultCheckBoxStyle}" EditingElementStyle="{DynamicResource DefaultCheckBoxStyle}"/>
                <DataGridTextColumn Header="Regra/ID" Binding="{Binding Id}" Width="250" IsReadOnly="True"/>
                <DataGridTextColumn Header="Severidade" Binding="{Binding Severity}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Type}" Width="70" IsReadOnly="True"/>
                <DataGridTextColumn Header="Arquivo" Binding="{Binding FileLocation}" Width="250" IsReadOnly="True"/>
                <DataGridTextColumn Header="Linha" Binding="{Binding Line}" Width="60" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="lblCount" Text="0 vulnerabilidades visíveis" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Button Content="Cancelar" Grid.Column="1" Margin="0,0,10,0" Padding="15,8" Click="BtnCancel_Click"/>
            <Button Content="Gerar Prompt Selecionado" Grid.Column="2" Style="{DynamicResource AccentButtonStyle}" Padding="15,8" Click="BtnGenerate_Click"/>
        </Grid>
    </Grid>
</Window>
